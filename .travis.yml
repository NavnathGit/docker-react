language: generic 
sudo: required  
services:
  - docker

before_install:
  - docker build -t navnathd/docker-react -f Dockerfile.dev . 

script:
  - docker run -e CI=true navnathd/docker-react npm run test
  
